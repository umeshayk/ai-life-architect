Read AGENTS.md and implement Section 8.12 Background Jobs.

Goal:
Create a robust job processing layer for asynchronous workloads with retries, monitoring, and operational visibility.

Implement:
- Ingestion jobs
- Embedding jobs
- AI summary jobs
- Recommendation refresh jobs
- Reminder generation
- Retry and monitoring support

Requirements:
- Build worker/job infrastructure.
- Add job records/status persistence if not already present.
- Support job types:
  - ingestion
  - embeddings
  - ai summaries
  - recommendation refresh
  - reminder generation
- Implement states:
  - queued
  - running
  - completed
  - failed
  - retry_scheduled
  - cancelled if appropriate
- Add retry policy controls and failure metadata.
- Add admin-visible monitoring endpoints.
- Ensure idempotency where possible.
- Integrate with existing modules that produce async work.

Frontend/admin:
- job list and job detail views in admin console
- filters by type/status/date
- retry action where safe
- operational stats summary

Testing:
- job scheduling
- state transitions
- retry logic
- monitoring queries
- frontend admin jobs view

Docs:
- docs/operations/background-jobs.md
- docs/modules/jobs.md

After implementation:
- run lint, type check, tests, build
- commit: "feat: implement section 8.12 background jobs"
- push