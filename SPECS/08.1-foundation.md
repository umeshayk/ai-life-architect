Read AGENTS.md and implement Section 8.1 Foundation as a production-grade enterprise baseline.

Goal:
Establish the full application foundation for a professional AI Life Architect platform using the locked stack and repo conventions from AGENTS.md. This is not a demo scaffold. Build a durable baseline that all later modules can extend cleanly.

Implement all of the following together:
1. Monorepo or clean multi-app structure
2. Docker Compose
3. Environment examples
4. README
5. Architecture docs
6. Health checks
7. Frontend app shell

Required architecture:
- Use the expected repository structure from AGENTS.md for backend and frontend modules.
- Keep backend and frontend clearly separated.
- Ensure the structure is ready for auth, domain modules, AI, admin, jobs, analytics, and docs.
- Avoid placeholder folders that do not connect to real implementation.

Backend requirements:
- Create backend application bootstrap with modular layout:
  - app/api/v1
  - app/core
  - app/db
  - app/models
  - app/schemas
  - app/services
  - app/modules/*
  - app/workers
  - app/utils
- Implement a health module with endpoints:
  - GET /health/live
  - GET /health/ready
  - GET /health/details
- Readiness must validate at minimum:
  - API boot success
  - database connectivity
  - background worker dependency readiness if configured
  - AI provider configuration presence only as non-fatal metadata
- Add structured response payloads for health endpoints.
- Add request ID / correlation ID middleware.
- Add structured logging setup.
- Add central config management using environment variables.
- Add startup validation for required environment variables.
- Add base exception handling and consistent API error shape.

Database requirements:
- Configure PostgreSQL connection.
- Prepare migration tooling.
- Add an initial migration baseline if needed.
- Prepare support for pgvector extension but do not fully implement retrieval yet.
- Ensure local/dev environment runs with Docker Compose.

Frontend requirements:
- Build a professional frontend app shell using Vite + React.
- Add:
  - responsive sidebar navigation
  - top header
  - page container layout
  - breadcrumb support
  - route placeholders for future modules
  - skeleton loader primitives
  - error boundary
  - empty state component
- Must support:
  - mobile
  - tablet
  - laptop
  - desktop
  - wide desktop
- Do not hardcode page widths or colors.
- Implement token-based design foundations from the start.
- Include light theme, dark theme, and one premium alternate theme.
- Add shared layout primitives rather than page-specific wrappers.

DevOps requirements:
- Create Docker Compose for:
  - frontend
  - backend
  - postgres
  - worker placeholder service if needed
- Add .env.example files for backend and frontend.
- Add Makefile or package scripts for:
  - local startup
  - migrations
  - tests
  - lint
  - build
- Add .gitignore appropriate for monorepo.
- Add seed/demo hooks placeholder without fake business logic.

Documentation requirements:
- Update/create:
  - root README.md
  - docs/architecture/overview.md
  - docs/architecture/backend.md
  - docs/architecture/frontend.md
  - docs/development/local-setup.md
- README must explain:
  - stack
  - project structure
  - how to run locally
  - environment files
  - docker startup
  - migration commands
  - test commands
- Architecture docs must show module boundaries and request flow.

Quality requirements:
- Must compile and run.
- Must pass lint/type checks.
- Must not leave dead scaffolding.
- Must include meaningful comments only where helpful.
- Must use enterprise naming and folder discipline.
- Must follow AGENTS responsive and theme rules exactly.

Testing:
- Add backend tests for health endpoints.
- Add frontend tests for app shell rendering, sidebar responsiveness, and theme switch behavior.
- Add one smoke e2e path:
  - app loads
  - sidebar renders
  - health status visible in admin/dev status area if you include one

Deliverables:
- actual code
- config
- docs
- tests
- any needed migration/bootstrap files

After implementation:
1. run lint
2. run type check
3. run tests
4. run build
5. fix any issues
6. update docs with final commands
7. commit with message: "feat: implement section 8.1 foundation"
8. push changes

Do not stop at scaffolding. Complete the implementation end-to-end.