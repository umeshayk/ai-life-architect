Read AGENTS.md and implement Section 8.15 Security Hardening.

Goal:
Harden the application across API, frontend, uploads, permissions, and defaults.

Implement:
- Rate limiting
- Secure uploads
- Ownership checks
- Permission enforcement
- Safe rendering paths
- Secure defaults

Requirements:
- Add API rate limiting for sensitive/auth-heavy endpoints first, with extensible global strategy.
- Lock down uploads:
  - file type validation
  - file size limits
  - storage isolation
  - filename sanitization
  - content-type distrust handling
- Add systematic ownership checks across user-scoped entities.
- Audit all routes for permission enforcement.
- Add safe rendering strategy for any rich text/markdown/document preview.
- Ensure secure defaults:
  - least privilege
  - no debug leakage
  - secure headers where applicable
  - safe CORS configuration
  - trusted validation patterns
- Review token handling and session invalidation.
- Add security-focused tests and docs.
- Add developer checklist for future modules.

Frontend:
- sanitize rendered content
- handle forbidden/not-found states safely
- no insecure local storage decisions unless deliberately justified
- no hidden-only security; server must enforce all real permissions

Testing:
- rate limit tests
- ownership/permission tests
- upload validation tests
- safe rendering tests
- security regression checks

Docs:
- docs/security/security-hardening.md
- update deployment docs with production secure settings

After implementation:
- run lint, type check, tests, build
- commit: "feat: implement section 8.15 security hardening"
- push