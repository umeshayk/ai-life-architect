Read AGENTS.md and implement Section 8.6 AI Orchestration.

Goal:
Create a durable AI orchestration layer with provider abstraction, Ollama integration, task routing, prompt templates, logging, safe fallback behavior, and failure handling.

Implement:
- Provider abstraction
- Ollama implementation
- Task-based routing
- Prompt template management
- AI request logs
- Failure handling
- Safety hooks

Backend requirements:
- Create ai module with:
  - provider interface
  - ollama provider implementation
  - task router
  - prompt template registry/storage
  - AI request log persistence
  - safety/policy hook layer
- Support task types such as:
  - summarization
  - planning suggestion
  - recommendation explanation
  - journal synthesis
  - relationship suggestion
- Implement model/provider configuration via env and admin-safe config.
- Add prompt template management:
  - versioned templates
  - variable interpolation
  - safe defaults
- Log AI requests with:
  - task type
  - provider
  - model
  - prompt template ID/version
  - latency
  - token estimates if available
  - status
  - failure reason
- Add timeout and retry control.
- Implement graceful fallback behavior so AI never blocks core deterministic flows.
- Add safety hooks before and after provider call.
- Cache safe deterministic outputs where useful.

Frontend requirements:
- Build admin/developer-facing visibility for AI request logs.
- Build settings/config screens only if appropriate for current architecture.
- Surface graceful fallback states in user UI.
- Clearly label AI-generated suggestions where relevant.

Testing:
- provider abstraction tests
- ollama adapter tests with mocks
- task routing tests
- failure fallback tests
- prompt rendering tests
- AI request log persistence tests
- frontend visibility tests where applicable

Docs:
- docs/modules/ai-orchestration.md
- docs/operations/ai-provider-setup.md
- document Ollama local setup and failure modes

After implementation:
- run lint, type check, tests, build
- commit: "feat: implement section 8.6 ai orchestration"
- push