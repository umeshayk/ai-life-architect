Read AGENTS.md and implement Section 8.3 Core Domain as the structured backbone of the platform.

Goal:
Build the main business entities and their enterprise-grade CRUD, listing, relationship, filtering, pagination, and cross-linking workflows.

Implement all of the following:
- CRUD + list/detail + filtering + sorting + pagination
- Workspaces
- Life areas
- Goals
- Projects
- Tasks
- Notes
- Journals
- Routines
- Events
- Tags
- Cross-linking between entities

Backend requirements:
- Create normalized models, migrations, services, schemas, and API endpoints for:
  - workspaces
  - life_areas
  - goals
  - projects
  - tasks
  - notes
  - journals
  - routines
  - events
  - tags
  - entity_tags
  - cross-link/reference table(s) if needed
- Add ownership and workspace scoping.
- Use created_at, updated_at, created_by, updated_by.
- Add soft delete where appropriate.
- Add status, priority, due dates, start dates, metadata JSONB where useful.
- Implement list endpoints with:
  - filtering
  - sorting
  - pagination
  - search query where useful
- Implement detail endpoints with related summaries.
- Support entity linking examples:
  - goal -> projects
  - project -> tasks
  - task -> notes
  - journal -> linked goals/life areas
  - event -> related project/task
- Support tag assignment across entities.
- Add validation rules for relationships and required fields.
- Add audit logging for create/update/delete/archive actions.

Frontend requirements:
- Build feature modules and routes for each core entity.
- Support:
  - list views
  - detail views
  - create/edit forms
  - filters
  - sorting
  - pagination
  - quick actions
  - linked-entity side panels or related-content sections
- Use consistent page layout patterns.
- Use drawers for edit/create where appropriate.
- Use tables on larger screens and stacked cards/adaptive views on small screens.
- Support breadcrumbs and entity detail navigation.
- Add empty states, skeletons, validation messages, optimistic updates only if safe.
- Make task/project/goal flows especially polished because they are central.

Functional expectations:
- Workspaces isolate user content.
- Life areas categorize goals and journaling.
- Goals can have milestones or child projects later; prepare extensible schema.
- Projects and tasks should support practical productivity workflows.
- Routines support recurrence metadata.
- Events support scheduling metadata.
- Journals and notes support rich text or markdown-safe storage strategy.

Testing:
- Backend integration tests for CRUD and list/query behavior for each major entity.
- Permission and ownership tests.
- Frontend tests for forms, list filters, pagination.
- E2E tests for:
  - create workspace
  - create life area
  - create goal
  - create project under goal
  - create task under project
  - link note and tags
  - verify details show related entities

Documentation:
- Create docs/modules/core-domain.md
- Update ERD / architecture docs.
- Document API conventions and entity relationships.

After implementation:
1. migrate schema
2. run lint, type check, tests, build
3. fix defects
4. commit with message: "feat: implement section 8.3 core domain"
5. push changes