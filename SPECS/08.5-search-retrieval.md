Read AGENTS.md and implement Section 8.5 Search and Retrieval.

Goal:
Build enterprise retrieval infrastructure with pgvector readiness, keyword search, semantic search abstraction, hybrid ranking, filters, and related content APIs/UI.

Implement:
- pgvector support
- Embedding abstraction
- Semantic search
- Keyword search
- Hybrid retrieval
- Filters
- Related content

Backend requirements:
- Enable pgvector extension and schema support.
- Add vector column(s) for document chunks or retrieval records.
- Build embedding provider abstraction.
- Do not hardcode to one provider.
- Implement keyword search using PostgreSQL full-text search or equivalent.
- Implement semantic search using vector similarity.
- Implement hybrid retrieval combining keyword + vector scores.
- Add retrieval filters:
  - workspace
  - entity type
  - tags
  - date range
  - owner
  - source type
- Add related content endpoint based on similarity/linkage.
- Return source attribution in results.
- Add ranking explainability fields in response for debugging/admin use.
- Add request logs for search activity where appropriate.

Frontend requirements:
- Build global search experience with:
  - query input
  - search results tabs or grouped sections
  - filter panel
  - relevance indicators
  - source snippets
  - related content panel
- Support searching across documents and structured entities if available.
- Must be responsive and quick to use.
- Include loading, empty, and no-result states.

Engineering expectations:
- Make embedding generation asynchronous where needed.
- Design search service to support future AI answer generation without rewriting the retrieval layer.
- Keep APIs clean and composable.

Testing:
- keyword search correctness
- semantic search mocked/provider-abstracted tests
- hybrid ranking tests
- filter behavior
- related content behavior
- frontend search/filter tests
- e2e search path with seeded data

Docs:
- docs/modules/search.md
- docs/architecture/retrieval.md
- note pgvector setup and migration steps

After implementation:
- run migrations
- run lint, type check, tests, build
- commit: "feat: implement section 8.5 search and retrieval"
- push