Read AGENTS.md and implement Section 8.7 Relationship / Graph Engine.

Goal:
Represent explicit and inferred relationships across user entities and documents, with confidence, attribution, and browseable graph APIs/UI.

Implement:
- Explicit relationships
- Suggested/inferred relationships
- Confidence score
- Source of relationship
- Graph browsing APIs and UI

Requirements:
- Add tables/models for:
  - relationships
  - relationship_suggestions
- Relationship records must support:
  - source entity
  - target entity
  - relationship type
  - directionality
  - confidence score
  - source kind (manual, rule, AI, ingestion)
  - justification/source attribution
  - created_by / approved_by where relevant
- Build APIs for:
  - create explicit relationship
  - list relationships for entity
  - browse graph neighborhood
  - review suggestion list
  - accept/reject suggestion
- Add inferred relationship generation hooks from:
  - shared tags
  - shared text similarity
  - same project/goal context
  - AI suggestion provider if enabled
- Frontend:
  - graph browse screen
  - entity relationship panel
  - suggestion review workflow
  - confidence and source display
- Keep UI usable even without a fancy graph canvas; functional clarity first.
- If using a graph visualization, ensure fallback list/table view exists.

Testing:
- relationship CRUD
- suggestion accept/reject
- confidence/source attribution persistence
- graph neighborhood queries
- frontend browse/review interaction

Docs:
- docs/modules/relationships.md
- explain explicit vs inferred relationship lifecycle

After implementation:
- run lint, type check, tests, build
- commit: "feat: implement section 8.7 relationship graph engine"
- push