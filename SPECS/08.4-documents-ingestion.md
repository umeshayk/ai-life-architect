Read AGENTS.md and implement Section 8.4 Documents and Ingestion.

Goal:
Enable secure document upload, metadata tracking, processing lifecycle management, chunk extraction pipeline hooks, retry support, and source attribution for later search/retrieval features.

Implement:
- File upload
- Metadata
- Processing status
- Background extraction/chunking
- Retry processing
- Source attribution

Requirements:
- Add tables/models/migrations for:
  - documents
  - document_versions if needed
  - document_chunks
  - ingestion_jobs
- Support upload metadata:
  - file name
  - mime type
  - size
  - checksum
  - owner
  - workspace
  - upload source
  - status
- Implement upload API with secure validation:
  - allowed types
  - size limits
  - storage path strategy
  - no executable upload risks
- Implement processing lifecycle:
  - uploaded
  - queued
  - processing
  - completed
  - failed
  - retrying
- Implement background job initiation after upload.
- Implement extraction/chunking pipeline abstraction so later OCR/parsing providers can be plugged in.
- Persist chunks with ordering and attribution back to source document/version/page/section if possible.
- Add retry endpoint or admin action for failed processing.
- Record processing errors and timestamps.
- Add source attribution fields for each chunk.
- Add audit logs for document upload/delete/retry.

Frontend:
- Build document list page and upload flow.
- Show:
  - upload progress
  - metadata
  - processing status badges
  - retry action
  - document detail view
  - chunk preview if available
- Add filters by status/type/workspace.
- Make upload UI enterprise-grade, not toy-like.
- Add clear failure/retry UX.

Background jobs:
- Integrate with worker architecture.
- Ensure jobs are idempotent where possible.
- Capture job status and error details.

Testing:
- upload success/failure
- metadata persistence
- processing status transitions
- retry flow
- permission checks
- frontend upload interaction
- e2e upload and processing mock path

Docs:
- docs/modules/documents.md
- docs/operations/ingestion.md
- update README with upload setup

After implementation:
- run lint, type check, tests, build
- commit: "feat: implement section 8.4 documents and ingestion"
- push