Read AGENTS.md and implement Section 8.2 Auth and RBAC as a production-grade authentication and authorization module.

Goal:
Build secure enterprise authentication with RBAC, auditability, session lifecycle support, and admin-manageable user access.

Implement all of the following:
- Login
- Refresh token flow
- Logout
- Current user endpoint
- User admin management
- Role and permission mapping
- Password hashing
- Auth audit logging

Backend requirements:
- Create auth module and users/admin user management module.
- Add normalized tables and migrations for:
  - users
  - roles
  - permissions
  - user_roles
  - role_permissions
  - auth_sessions or refresh_tokens
  - audit_logs
- Include timestamps, created_by, updated_by where relevant.
- Support soft delete or deactivate for users where appropriate.
- Use secure password hashing with modern defaults.
- Implement login with:
  - email/username + password
  - access token
  - refresh token
  - invalid credential handling
  - disabled user handling
- Implement refresh flow with rotation or secure revocation logic.
- Implement logout:
  - current session logout
  - optionally all sessions logout if architecture supports it
- Implement current user endpoint.
- Implement user admin endpoints:
  - create user
  - list users
  - get user detail
  - update user
  - activate/deactivate user
  - assign/remove roles
  - reset password flow placeholder or admin reset endpoint
- Implement RBAC enforcement middleware/dependencies:
  - route-level permission enforcement
  - role-permission mapping
  - ownership checks where role alone is insufficient
- Record audit logs for:
  - login success/failure
  - logout
  - token refresh
  - password changes/resets
  - user creation/update/deactivation
  - role assignment/removal

Security requirements:
- Never expose password hashes.
- Use secure cookie or token strategy consistently.
- Add brute-force/rate-limit hooks if foundation already supports them.
- Validate all auth inputs.
- Return consistent secure error messages.
- Prevent privilege escalation in admin endpoints.
- Ensure refresh tokens are revocable and tracked.

Frontend requirements:
- Build:
  - login page
  - auth loading bootstrap
  - protected routes
  - session refresh handling
  - logout action
  - current-user store
  - access-denied state
- Build admin user management UI:
  - user list
  - filters
  - user detail/edit drawer/page
  - role assignment UI
  - active/inactive status controls
- Show role/permission-aware navigation.
- Add graceful expired-session handling.
- Add loading, empty, and error states.
- Must be responsive and work across themes.

API requirements:
- Clear request/response schemas.
- Use secure DTOs.
- Do not leak internal token metadata unnecessarily.
- Support pagination/filtering/sorting for user admin list.

Testing:
- Backend tests:
  - login success/failure
  - refresh success/failure
  - logout
  - current user endpoint
  - permission enforcement
  - admin-only endpoints
  - audit log creation
- Frontend tests:
  - login flow
  - protected route redirect
  - logout flow
  - RBAC-based UI hiding/showing
- E2E:
  - login as admin
  - view user list
  - assign role
  - logout
  - login as restricted user
  - confirm denied access to admin screen

Documentation:
- Update README auth section.
- Create docs/security/authentication.md
- Create docs/modules/auth.md
- Document token lifecycle and RBAC model.

After implementation:
1. run migrations
2. run lint, type check, tests, build
3. fix all issues
4. commit with message: "feat: implement section 8.2 auth and rbac"
5. push changes